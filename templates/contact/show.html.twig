{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <h1>Contact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Név</th>
                <td>{{ contact.name }}</td>
            </tr>
            <tr>
                <th>E-mail</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Üzenet</th>
                <td>{{ contact.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact_index') }}">Üzenetek megtekintése</a>

    <a href="{{ path('contact_edit', {'id': contact.id}) }}">Szerkeszt</a>

    {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
